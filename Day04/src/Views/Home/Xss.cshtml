@{
    ViewData["Title"] = "XSS Sample";
}
<h3>@ViewData["Title"]</h3>
<div class="row">
    <p>Has Hijacking usando XSS (no le digas a mi jefe que dejo esto por aquí 😅</p>
</div>
<br />
<div class="row">
    <form asp-action="Xss" method="get">
        <div class="form-group">
            <label for="comment" class="control-label">Comment</label>
            <input class="form-control" id="comment" name="comment"/>
        </div>

        <div class="form-group">
            <input type="submit" value="Submit"/>
        </div>
    </form>
    @if (!string.IsNullOrEmpty(ViewBag.Comment))
    {
        <div class="row">
            <div class="col-xs-12">
                <b>Text sent: "@Html.Raw(ViewData["Comment"])"</b>
            </div>
        </div>
    }
</div>